server {
  listen 0.0.0.0:{{ project_port }};
  root {{ project_install_path }}/{{ project_name }};
  index index.html;
  location /adf {
    autoindex on;
    try_files $uri $uri/ /index.html;
  }
}

